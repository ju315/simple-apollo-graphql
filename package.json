{
  "name": "simpole-graphql",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run dev",
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child src/index.ts",
    "generate": "ts-node --transpile-only src/schema/schema"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^3.9.0",
    "graphql": "^16.5.0",
    "nexus": "^1.3.0"
  },
  "devDependencies": {
    "ts-node": "^10.8.2",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.7.4"
  }
}
